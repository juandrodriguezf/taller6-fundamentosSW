package com.taller6.solid.i;

public interface CalculadoraConfigurador {
    void registrar(String nombre, int aridad, Calculo calculo);
    // Podrían haber métodos para eliminar u otras configuraciones de administración
}
